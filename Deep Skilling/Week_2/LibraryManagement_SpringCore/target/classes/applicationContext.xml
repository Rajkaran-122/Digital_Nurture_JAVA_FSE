<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="
           http://www.springframework.org/schema/beans 
           http://www.springframework.org/schema/beans/spring-beans.xsd
           http://www.springframework.org/schema/context 
           http://www.springframework.org/schema/context/spring-context.xsd
           http://www.springframework.org/schema/aop 
           http://www.springframework.org/schema/aop/spring-aop.xsd">

    <!-- Exercise 6: Enable Component Scanning for annotations (@Service, @Repository) -->
    <context:component-scan base-package="com.library" />

    <!-- Exercise 3 & 8: Enable AspectJ Auto Proxying for @Aspect -->
    <aop:aspectj-autoproxy />

    <!-- 
       Exercises 1, 2, 5, 7: XML-based bean configuration 
       Note: We are explicitly defining these to satisfy the exercises, 
       even though component scanning is also enabled.
       We will name them differently to avoid conflicts with annotation beans.
    -->
    
    <!-- Repository Bean -->
    <bean id="xmlBookRepository" class="com.library.repository.BookRepository" />

    <!-- Service Bean with Setter Injection (Ex 2, 5, 7) -->
    <bean id="xmlBookServiceSetter" class="com.library.service.BookService">
        <property name="bookRepository" ref="xmlBookRepository" />
    </bean>

    <!-- Service Bean with Constructor Injection (Ex 7) -->
    <bean id="xmlBookServiceConstructor" class="com.library.service.BookService">
        <constructor-arg ref="xmlBookRepository" />
    </bean>

</beans>
